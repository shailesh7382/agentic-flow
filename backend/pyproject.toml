[project]
name = "local-agentic-flow"
version = "0.1.0"
description = "FastAPI and LangGraph backend for a local LM Studio agent"
readme = "../README.md"
requires-python = ">=3.11"
dependencies = [
  "asyncssh==2.24.0",
  "fastapi==0.139.2",
  "httpx==0.28.1",
  "langchain==1.3.14",
  "langchain-openai==1.3.5",
  "langgraph==1.2.9",
  "openai==2.46.0",
  "oracledb==4.0.2",
  "psutil==7.2.2",
  "pydantic-settings==2.11.0",
  "sqlglot==30.13.0",
  "uvicorn[standard]==0.51.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
  "pytest==8.4.2",
  "pytest-asyncio==1.2.0",
  "ruff==0.14.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
